<Controls:MetroWindow
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:ee="http://schemas.microsoft.com/expression/2010/effects"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
    xmlns:views="clr-namespace:WpfClient.Views"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:cmd="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.WPF45"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WpfClient" x:Class="WpfClient.MainWindow"
    Title="Collections" Height="852.5" Width="1844.5" ShowIconOnTitleBar="True" ResizeMode="CanResizeWithGrip">
    <Controls:MetroWindow.Resources>

    </Controls:MetroWindow.Resources>

    <Controls:MetroWindow.WindowCommands>
        <Controls:WindowCommands>
            <Button Content="Settings" Command="{Binding CmdSettings}" />
            <Button Content="About" Command="{Binding CmdAbout}" />
        </Controls:WindowCommands>

    </Controls:MetroWindow.WindowCommands>

    <Controls:MetroWindow.Flyouts>
        <Controls:FlyoutsControl>

            <local:SettingsFlyout Header="Settings" Position="Right" Width="500" CanCloseFlyout="True" />
            <local:AboutFlyout Header="About" Position="Right" Width="500" CanCloseFlyout="True" />
        </Controls:FlyoutsControl>
    </Controls:MetroWindow.Flyouts>


    <Grid>

        <DockPanel>
            <Grid DockPanel.Dock="Bottom">
                <Grid.Resources>
                    <ResourceDictionary
                        Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.AnimatedTabControl.xaml" />
                </Grid.Resources>
                <TabControl x:Name="TabControl" SelectionChanged="TabControl_SelectionChanged">

                    <Controls:MetroTabItem Header="Explore" x:Name="Tab0">
                        <views:ExploreMode />

                    </Controls:MetroTabItem>
                    <Controls:MetroTabItem Header="Play" x:Name="Tab1">
                        <views:PlayMode />
                    </Controls:MetroTabItem>
                    <Controls:MetroTabItem Header="Other" x:Name="Tab2">
                        <TextBlock FontSize="30" Text="Not yet implemented" />
                    </Controls:MetroTabItem>
                </TabControl>

            </Grid>

        </DockPanel>


    </Grid>
</Controls:MetroWindow>